#!/bin/bash -l

# crontab -e        # to edit the command for schedule.
# crontab -l        # to show the configuration for crontab

echo "Start to run a script."
sleep 100                                                       # After 100s
echo "The end of the shell script."

####################################################################################################
#
# Put the schedule in the script and add it to crontab to execute at assigned time.
#
####################################################################################################
